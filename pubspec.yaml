name: flutter_waya
description: The Flutter UI library contains multiple custom src,compatible with android,web,ios and MAC.
version: 9.3.0
repository: https://github.com/Wayaer/flutter_waya

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.16.0'

dependencies:
  flutter:
    sdk: flutter
  fl_extended: ^0.4.0

dev_dependencies:
  flutter_lints: ^3.0.2