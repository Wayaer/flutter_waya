name: waya
description: Demonstrates how to use the waya plugin.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.10.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_curiosity: ^2.0.0
  flutter_localizations:
    sdk: flutter
  flutter_waya:
    path: ../
  flutter_web_plugins:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - lib/res/banner/